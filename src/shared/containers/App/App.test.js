import React from "react";
import TestUtils from "react/lib/ReactTestUtils";
// import App from "./App"

describe("App", function () {

  let renderedComponent = null;
  let nav = null;

  beforeEach(function() {

    // renderedComponent = TestUtils.renderIntoDocument(
    //   <App />
    // );
    //
    // nav = TestUtils.findRenderedDOMComponentWithTag(renderedComponent, "nav");
    // container = TestUtils.findRenderedDOMComponentWithClassName(renderedComponent, "container");
    // undoButton = TestUtils.findRenderedDOMComponentWithClassName(renderedComponent, "undo-button");
    // redoButton = TestUtils.findRenderedDOMComponentWithClassName(renderedComponent, "redo-button");

  });

  it("renders without problems", function () {

    // expect(TestUtils.isDOMComponent(nav)).toBe(true);
    // expect(TestUtils.isDOMComponent(container)).toBe(true);
    // expect(TestUtils.isDOMComponent(undoButton)).toBe(true);
    // expect(TestUtils.isDOMComponent(redoButton)).toBe(true);

  });

});